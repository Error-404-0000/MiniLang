fn number FileExists(path){
    give win io FileExists(path);
}

fn string ReadText(path){
    give win io ReadText(path);
}

fn nothing WriteText(path, content){
    win io WriteText(path, content);
}

fn nothing EnsureDirectory(path){
    win io EnsureDirectory(path);
}
